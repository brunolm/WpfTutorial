<UserControl x:Class="WpfTutorial.Views.Level7View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Item name" />
                <TextBox Text="{Binding ItemName}" Width="150" />
                <Button Content="Add" Command="" />
                <Button Content="Remove" Command="" />
            </StackPanel>
            <DataGrid ItemsSource="{Binding WishListItems}" SelectedItem="{Binding SelectedWishlistItem}" />
        </StackPanel>
    </Grid>
</UserControl>
